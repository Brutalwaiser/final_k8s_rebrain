apiVersion: v1
kind: Secret
metadata:
  name: librespeed-secret
  namespace: final
  labels:
    app.kubernetes.io/instance: librespeed
type: Opaque
stringData:
  # Пароль к MySQL (совпадает с MYSQL_PASSWORD в db/mysql-secret).
  DB_PASSWORD: "speedSecret123"
  # Пароль к странице статистики /results/stats.php
  PASSWORD: "statsSecret123"
